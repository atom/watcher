language: node_js
node_js:
- 8.1.4
sudo: false
dist: trusty

addons:
  apt:
    packages:
    - clang-5.0
    - build-essential

git:
  depth: 10

branches:
  only:
  - master

script:
- npm run ci:travis
- npm run lint
- npm run format && git diff

env:
  global:
  - CC=clang
  - CXX=clang++
  - npm_config_clang=1

notifications:
  email:
    on_success: never
    on_failure: change
